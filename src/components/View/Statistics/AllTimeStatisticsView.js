import React from "react";

const AllTimeStatisticsView = ({ getStatisticsData }) => {

    const data = getStatisticsData();

    return (
        <div className="view">

        </div>
    );
}

AllTimeStatisticsView.displayName = 'AllTimeStatisticsView';
export default AllTimeStatisticsView;
